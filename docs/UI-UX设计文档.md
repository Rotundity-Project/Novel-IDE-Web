# Novel-IDE-Web UI/UX 设计文档

## 1. 设计概述

### 1.1 设计理念
- **类 IDE 体验** - 模仿 VSCode、Cursor 等现代 IDE 的界面风格
- **沉浸式写作** - 专注内容，减少视觉干扰
- **高效工作流** - 快捷键、多面板、智能提示
- **可定制性** - 主题、布局、字体高度可定制

### 1.2 目标用户
- 小说创作者
- 网文作者
- 业余写作爱好者
- 内容创作者

### 1.3 使用场景
- 长篇创作
- AI 辅助写作
- 角色与世界观管理
- 多作品并行创作

---

## 2. 视觉设计

### 2.1 色彩系统

#### 主色调（深色主题）
```css
/* 基础色板 */
--bg-primary: #1e1e1e;          /* 主背景 */
--bg-secondary: #252526;       /* 次要背景 */
--bg-tertiary: #2d2d2d;         /* 第三背景 */
--bg-hover: #2a2d2e;            /* 悬停背景 */

--text-primary: #cccccc;        /* 主文本 */
--text-secondary: #858585;      /* 次要文本 */
--text-muted: #6a6a6a;          /* 弱化文本 */

--border-color: #3c3c3c;        /* 边框 */
--border-subtle: #333333;       /* 细边框 */

/* 功能色 */
--accent-primary: #0078d4;      /* 主强调色（类 VSCode 蓝色） */
--accent-secondary: #0e639c;    /* 次强调色 */
--accent-success: #4ec9b0;      /* 成功 */
--accent-warning: #dcdcaa;     /* 警告 */
--accent-error: #f14c4c;        /* 错误 */
--accent-info: #75beff;         /* 信息 */

/* AI 专用色 */
--ai-primary: #c586c0;          /* AI 标识色（紫色） */
--ai-secondary: #9cdcfe;        /* AI 辅助色 */
```

#### 浅色主题
```css
--bg-primary: #ffffff;
--bg-secondary: #f3f3f3;
--bg-tertiary: #ececec;
--text-primary: #333333;
--text-secondary: #616161;
--accent-primary: #0067c0;
```

#### 语义色
```css
--color-danger: #da3633;        /* 危险操作 */
--color-caution: #d29922;       /* 谨慎操作 */
--color-neutral: #6e7781;       /* 中性操作 */
--color-success: #1a7f37;       /* 成功操作 */
```

### 2.2 字体系统

#### 中文字体
```css
--font-primary: "PingFang SC", "Microsoft YaHei", "Heiti SC", sans-serif;
--font-serif: "Songti SC", "SimSun", "Georgia", serif;
--font-mono: "JetBrains Mono", "Fira Code", "Consolas", monospace;
```

#### 字号层级
```css
--text-xs: 12px;    /* 辅助信息 */
--text-sm: 13px;    /* 次要文本 */
--text-base: 14px;  /* 基础文本 */
--text-lg: 16px;    /* 强调文本 */
--text-xl: 18px;    /* 标题小 */
--text-2xl: 20px;   /* 标题中 */
--text-3xl: 24px;   /* 标题大 */
--text-4xl: 32px;   /* 标题超大 */
```

#### 行高
```css
--leading-tight: 1.25;
--leading-normal: 1.5;
--leading-relaxed: 1.75;
--leading-loose: 2.0;
```

### 2.3 间距系统
```css
--space-1: 4px;
--space-2: 8px;
--space-3: 12px;
--space-4: 16px;
--space-5: 20px;
--space-6: 24px;
--space-8: 32px;
--space-10: 40px;
--space-12: 48px;
```

### 2.4 圆角
```css
--radius-sm: 4px;
--radius-md: 6px;
--radius-lg: 8px;
--radius-xl: 12px;
--radius-full: 9999px;
```

### 2.5 阴影
```css
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.2);
--shadow-md: 0 4px 6px rgba(0, 0, 0, 0.3);
--shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.4);
--shadow-xl: 0 20px 25px rgba(0, 0, 0, 0.5);
```

---

## 3. 布局设计

### 3.1 主界面布局

```
┌─────────────────────────────────────────────────────────────────────┐
│  菜单栏 (32px)                                                        │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐                                 │
│  │文件│ │编辑│ │视图│ │ AI │ │帮助│                                 │
└─────────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────────┐
│  工具栏 (40px)                                                       │
│  ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌──────┐ ┌──┐                            │
│  │+│ │💾│ │↩│ │↪│ │🔍│ │🤖│ │ AI │ │☰│                            │
└─────────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────────┐
│  主工作区 (flex: 1)                                                  │
│  ┌──────────┬────────────────────────────┬────────────────────────┐ │
│  │          │                            │                        │ │
│  │  资源    │     编辑器主区域            │      大纲面板          │ │
│  │  管理器  │                            │                        │ │
│  │          │  ┌──────────────────────┐ │    📋 大纲            │ │
│  │ 📁 作品集│  │                      │ │                        │ │
│  │ 📖 第一章│  │                      │ │  第一章  开始          │ │
│  │ 📖 第二章│  │     正文内容编辑     │ │  第二章  发展          │ │
│  │ 📖 第三章│  │                      │ │  第三章  高潮          │ │
│  │          │  │                      │ │                        │ │
│  │ 👥 角色  │  │                      │ │    📝 标签            │ │
│  │ 🌍 场景  │  │                      │ │  #动作  #对话         │ │
│  │ 🏷️ 标签  │  │                      │ │                        │ │
│  │          │  └──────────────────────┘ │                        │ │
│  │ 🔍 搜索  │                            │                        │ │
│  │          │                            │                        │ │
│  │          │                            │                        │ │
│  └──────────┴────────────────────────────┴────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────────┐
│  状态栏 (24px)                                                        │
│  📄 第一章开始  |  字数: 3,245  |  💾 已保存  |  🤖 AI 就绪  |       │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 资源管理器

#### 结构
```
📁 我的作品
  📖 科幻小说
    📄 第一章 开始
    📄 第二章 飞船
    📄 第三章 外星接触
  📖 都市小说
    📄 第一章 相遇
📁 角色
  👤 张三（主角）
  👤 李四（配角）
📁 场景
  🌍 地球
  🌍 飞船内部
📁 标签
  🏷️ 动作
  🏷️ 对话
  🏷️ 情感
```

#### 交互
- 点击展开/折叠
- 右键菜单（新建、删除、重命名）
- 拖拽排序（章节）
- 搜索过滤

### 3.3 编辑器主区域

#### 工具栏
```
[加粗] [斜体] [下划线] [删除线]  |  [标题1] [标题2] [标题3]  |
[引用] [代码] [链接] [图片]  |  [AI续写] [AI改写] [AI分析]
```

#### 编辑区
- 行号显示
- 代码高亮（Markdown 语法）
- 拼写检查
- 光标平滑滚动
- 选中文本悬浮操作栏

### 3.4 大纲面板

#### 章节导航
```typescript
interface ChapterOutline {
  id: string;
  title: string;
  order: number;
  wordCount: number;
  children?: ChapterOutline[];
}
```

#### 标签云
```
#动作 (45)  #对话 (32)  #情感 (28)  #描写 (20)
```

### 3.5 AI 面板

#### AI 聊天模式
```
┌─────────────────────────────────────┐
│  🤖 AI 助手                    [×] │
├─────────────────────────────────────┤
│                                     │
│  👤 用户: 帮我写一段战斗场景        │
│                                     │
│  🤖 AI: 好的，根据当前情节...      │
│         [插入文档] [重新生成]      │
│                                     │
│  [输入框...]                    [📤]│
└─────────────────────────────────────┘
```

#### AI 功能选择
```
┌─────────────────────────────────────┐
│  选择 AI 功能                       │
├─────────────────────────────────────┤
│  📝 智能续写                        │
│  ✍️ 内容改写  [润色|扩写|精简]      │
│  🎨 风格转换                        │
│  💡 情节建议                        │
│  💬 对话生成                        │
│  🔍 内容分析                        │
└─────────────────────────────────────┘
```

---

## 4. 组件设计

### 4.1 按钮

#### 主按钮
```css
.btn-primary {
  background: var(--accent-primary);
  color: white;
  padding: 8px 16px;
  border-radius: 4px;
  border: none;
  font-weight: 500;
  transition: all 0.2s;
}

.btn-primary:hover {
  background: var(--accent-secondary);
}
```

#### 次按钮
```css
.btn-secondary {
  background: var(--bg-tertiary);
  color: var(--text-primary);
  border: 1px solid var(--border-color);
}
```

#### 图标按钮
```css
.btn-icon {
  background: transparent;
  color: var(--text-primary);
  padding: 6px;
  border-radius: 4px;
}

.btn-icon:hover {
  background: var(--bg-hover);
}
```

### 4.2 输入框

```css
.input {
  background: var(--bg-primary);
  border: 1px solid var(--border-color);
  border-radius: 4px;
  padding: 8px 12px;
  color: var(--text-primary);
  font-size: 14px;
}

.input:focus {
  border-color: var(--accent-primary);
  outline: none;
  box-shadow: 0 0 0 2px rgba(0, 120, 212, 0.2);
}
```

### 4.3 下拉菜单

```css
.dropdown {
  background: var(--bg-secondary);
  border: 1px solid var(--border-color);
  border-radius: 4px;
  box-shadow: var(--shadow-lg);
  max-height: 300px;
  overflow-y: auto;
}

.dropdown-item {
  padding: 8px 12px;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 8px;
}

.dropdown-item:hover {
  background: var(--bg-hover);
}

.dropdown-item.active {
  background: var(--bg-tertiary);
  color: var(--accent-primary);
}
```

### 4.4 模态框

```css
.modal-overlay {
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(4px);
}

.modal {
  background: var(--bg-secondary);
  border: 1px solid var(--border-color);
  border-radius: 8px;
  box-shadow: var(--shadow-xl);
  max-width: 600px;
  width: 90%;
}

.modal-header {
  padding: 16px 20px;
  border-bottom: 1px solid var(--border-color);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.modal-body {
  padding: 20px;
}

.modal-footer {
  padding: 16px 20px;
  border-top: 1px solid var(--border-color);
  display: flex;
  justify-content: flex-end;
  gap: 8px;
}
```

### 4.5 提示框

#### 消息提示
```css
.toast {
  background: var(--bg-secondary);
  border: 1px solid var(--border-color);
  border-left: 4px solid var(--accent-primary);
  border-radius: 4px;
  padding: 12px 16px;
  box-shadow: var(--shadow-lg);
  animation: slideIn 0.3s ease;
}

@keyframes slideIn {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}
```

#### 确认对话框
```
┌─────────────────────────────────────┐
│  ⚠️ 确认删除                       │
├─────────────────────────────────────┤
│  确定要删除"第一章"吗？此操作       │
│  不可撤销。                         │
│                                     │
│        [取消]  [删除]               │
└─────────────────────────────────────┘
```

### 4.6 加载状态

#### 骨架屏
```css
.skeleton {
  background: linear-gradient(
    90deg,
    var(--bg-secondary) 0%,
    var(--bg-tertiary) 50%,
    var(--bg-secondary) 100%
  );
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
}

@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
```

#### 加载指示器
```css
.spinner {
  border: 2px solid var(--bg-tertiary);
  border-top-color: var(--accent-primary);
  border-radius: 50%;
  width: 20px;
  height: 20px;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}
```

---

## 5. 页面设计

### 5.1 登录页

```
┌─────────────────────────────────────────────┐
│                                             │
│          Novel IDE                           │
│                                             │
│     ┌─────────────────────────────┐         │
│     │                             │         │
│     │  邮箱                       │         │
│     │  [输入邮箱...]            │         │
│     │                             │         │
│     │  密码                       │         │
│     │  [输入密码...]            │         │
│     │                             │         │
│     │  [    登录    ]             │         │
│     │                             │         │
│     │  还没有账号？[立即注册]      │         │
│     └─────────────────────────────┘         │
│                                             │
│     [GitHub] [Google] [微信]               │
│                                             │
└─────────────────────────────────────────────┘
```

### 5.2 注册页

```
┌─────────────────────────────────────────────┐
│          创建账号                           │
│                                             │
│     用户名: [输入用户名...]                │
│     邮箱:   [输入邮箱...]                  │
│     密码:   [输入密码...]                  │
│     确认:   [再次输入...]                  │
│                                             │
│     [    注册    ]                         │
│                                             │
└─────────────────────────────────────────────┘
```

### 5.3 作品列表页

```
┌─────────────────────────────────────────────┐
│  我的作品                    [+ 新建作品]  │
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ ┌─────┐  科幻小说                  │   │
│  │ │封面 │  15 章节 | 45,000 字        │   │
│  │ └─────┘  更新于 2026-02-10          │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ ┌─────┐  都市小说                  │   │
│  │ │封面 │  8 章节 | 23,000 字         │   │
│  │ └─────┘  更新于 2026-02-08          │   │
│  └─────────────────────────────────────┘   │
│                                             │
└─────────────────────────────────────────────┘
```

### 5.4 编辑器页

**见 3.1 主界面布局**

### 5.5 智能体管理页

```
┌─────────────────────────────────────────────┐
│  智能体管理                   [+ 新建智能体]│
├─────────────────────────────────────────────┤
│                                             │
│  [全部] [内置] [自定义] [公开]             │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 🤖 科幻小说专家         [⭐⭐⭐⭐⭐]  │   │
│  │ 专注于科幻小说创作...                 │   │
│  │ 使用次数: 1,250 | 评分: 4.8           │   │
│  │                         [使用] [编辑] │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 🤖 古风小说专家         [⭐⭐⭐⭐]   │   │
│  │ 擅长古代背景的叙事...                 │   │
│  │ 使用次数: 890 | 评分: 4.5             │   │
│  │                         [使用] [编辑] │   │
│  └─────────────────────────────────────┘   │
│                                             │
└─────────────────────────────────────────────┘
```

### 5.6 智能体编辑页

```
┌─────────────────────────────────────────────┐
│  编辑智能体: 科幻小说专家        [保存][取消]│
├─────────────────────────────────────────────┤
│                                             │
│  基本信息                                   │
│  名称: [科幻小说专家]                       │
│  描述: [专注于科幻小说创作...]             │
│  分类: [内置 ▼] [子分类 ▼]                │
│                                             │
│  系统提示词                                 │
│  ┌─────────────────────────────────────┐ │
│  │ 你是一个专业的科幻小说写作助手...     │ │
│  │                                     │ │
│  │                                     │ │
│  └─────────────────────────────────────┘ │
│                                             │
│  行为参数                                   │
│  创意度: [━━━━━━━━●━] 0.7                │
│  严谨度: [━━━━━●━━━━━] 0.5                │
│  细节度: [━━━━━━●━━━━] 0.6                │
│                                             │
│  知识领域                                   │
│  [+ 科学知识] [+ 未来科技] [+ 宇宙探索]     │
│                                             │
│  模型偏好                                   │
│  [GPT-4 Turbo ▼]                          │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 6. 交互设计

### 6.1 快捷键

#### 全局快捷键
| 快捷键 | 功能 |
|--------|------|
| `Ctrl/Cmd + S` | 保存 |
| `Ctrl/Cmd + N` | 新建作品/章节 |
| `Ctrl/Cmd + O` | 打开作品 |
| `Ctrl/Cmd + P` | 命令面板 |
| `Ctrl/Cmd + ,` | 设置 |
| `Ctrl/Cmd + K` | AI 面板 |

#### 编辑器快捷键
| 快捷键 | 功能 |
|--------|------|
| `Ctrl/Cmd + B` | 加粗 |
| `Ctrl/Cmd + I` | 斜体 |
| `Ctrl/Cmd + U` | 下划线 |
| `Ctrl/Cmd + Shift + S` | 删除线 |
| `Ctrl/Cmd + /` | 注释 |
| `Ctrl/Cmd + D` | 删除当前行 |

#### AI 快捷键
| 快捷键 | 功能 |
|--------|------|
| `Ctrl/Cmd + Enter` | AI 续写 |
| `Ctrl/Cmd + Shift + R` | AI 改写 |
| `Ctrl/Cmd + Shift + A` | AI 分析 |
| `Ctrl/Cmd + Shift + C` | AI 聊天 |

### 6.2 拖拽交互

#### 章节排序
- 支持拖拽章节调整顺序
- 拖拽时显示插入位置指示器
- 释放后保存新顺序

#### 文件拖入
- 拖入图片上传
- 拖入文档导入

### 6.3 悬停交互

#### 文本选中
- 选中文本显示悬浮操作栏
- 可选 AI 改写、复制、删除等操作

#### 角色提及
- 鼠标悬停在角色名上显示角色卡片
```
┌─────────────────────────────┐
│ 👤 张三（主角）            │
├─────────────────────────────┤
│ 年龄: 25岁                 │
│ 性别: 男                   │
│ 性格: 勇敢、善良           │
│                             │
│ [查看详情] [插入文本]      │
└─────────────────────────────┘
```

### 6.4 AI 流式输出

#### 输出动画
- 文字逐字显示
- 打字机效果
- 可随时停止

#### 输出完成
- 显示"完成"提示
- 提供操作按钮：插入、重新生成、编辑

### 6.5 版本历史时间线

```
┌─────────────────────────────────────┐
│  📅 版本历史                         │
├─────────────────────────────────────┤
│  ● 现在  14:32                      │
│     AI 生成  字数: 3,245           │
│     [查看] [恢复]                   │
│                                     │
│  ○ 14:20  手动保存                 │
│     字数: 2,890                     │
│     [查看] [恢复]                   │
│                                     │
│  ○ 13:45  AI 生成                 │
│     字数: 2,500                     │
│     [查看] [恢复]                   │
└─────────────────────────────────────┘
```

---

## 7. 响应式设计

### 7.1 断点

```css
--screen-xs: 480px;   /* 手机竖屏 */
--screen-sm: 640px;   /* 手机横屏 */
--screen-md: 768px;   /* 平板竖屏 */
--screen-lg: 1024px;  /* 平板横屏 */
--screen-xl: 1280px;  /* 桌面 */
--screen-2xl: 1536px; /* 大屏 */
```

### 7.2 平板适配

#### 布局调整
- 侧边栏可折叠
- 面板可拖动堆叠
- 触摸友好的操作

### 7.3 移动端适配（未来）

#### 布局调整
- 单栏布局
- 底部导航栏
- 简化的编辑器

---

## 8. 无障碍设计

### 8.1 键盘导航
- Tab 键顺序合理
- 焦点指示清晰
- 快捷键可自定义

### 8.2 屏幕阅读器
- 语义化 HTML
- ARIA 标签
- Alt 文本

### 8.3 对比度
- 文本对比度 ≥ 4.5:1
- 大文本对比度 ≥ 3:1

---

## 9. 动画与过渡

### 9.1 过渡时长

```css
--duration-fast: 150ms;
--duration-normal: 300ms;
--duration-slow: 500ms;
```

### 9.2 缓动函数

```css
--ease-in: cubic-bezier(0.4, 0, 1, 1);
--ease-out: cubic-bezier(0, 0, 0.2, 1);
--ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
```

### 9.3 动画示例

#### 面板滑入
```css
.slide-in-right {
  animation: slideInRight 0.3s var(--ease-out);
}

@keyframes slideInRight {
  from {
    transform: translateX(100%);
  }
  to {
    transform: translateX(0);
  }
}
```

#### 淡入淡出
```css
.fade-in {
  animation: fadeIn 0.2s var(--ease-out);
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
```

---

## 10. 错误处理

### 10.1 错误状态

#### 网络错误
```
┌─────────────────────────────────────┐
│                                     │
│     ❌ 网络连接失败                 │
│                                     │
│     无法连接到服务器，请检查       │
│     您的网络连接。                 │
│                                     │
│        [重试]    [刷新]            │
│                                     │
└─────────────────────────────────────┘
```

#### AI 错误
```
┌─────────────────────────────────────┐
│                                     │
│     🤖 AI 服务暂时不可用            │
│                                     │
│     生成失败，可能是上下文过长       │
│     或服务暂时繁忙。请稍后重试。     │
│                                     │
│        [重试]    [联系支持]          │
│                                     │
└─────────────────────────────────────┘
```

### 10.2 错误提示

#### 行内错误
```
输入框 [错误信息文本]
       ───────────
```

#### Toast 错误
```
[!] 错误信息文本
```

---

**文档版本**: v1.0
**最后更新**: 2026-02-11
