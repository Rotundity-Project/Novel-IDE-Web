# Novel-Studio-Web 部署文档

> 说明：仓库当前处于 Phase 1 骨架阶段，推荐以 `docker/docker-compose.yml` 为准完成本地/单机部署。
> 本文中若出现 `client/`、`VITE_*`、`prisma` 等内容，属于历史规划草案，后续会逐步对齐到当前仓库（Next.js app/ + Fastify server/ + Drizzle）。

## 0. Phase 1 快速部署（推荐）

### 0.1 依赖
- Docker Desktop / Docker Engine
- Docker Compose v2

### 0.2 一键启动（Postgres + Redis + server + app）

```bash
cp .env.example .env
docker compose -f docker/docker-compose.yml --env-file .env up --build -d
```

默认端口（可在 `.env` 覆盖）：
- app: 3000
- server: 3001
- postgres: 5432
- redis: 6379

## 1. 部署概述

### 1.1 部署架构

```
                          ┌─────────────┐
                          │   用户端    │
                          └──────┬──────┘
                                 │
                          ┌──────▼──────┐
                          │    CDN      │
                          │  (静态资源)  │
                          └──────┬──────┘
                                 │
                    ┌────────────▼────────────┐
                    │     负载均衡 (Nginx)   │
                    └────────────┬────────────┘
                                 │
           ┌─────────────────────┼─────────────────────┐
           │                     │                     │
    ┌──────▼──────┐      ┌──────▼──────┐      ┌──────▼──────┐
    │  应用服务器 1 │      │  应用服务器 2 │      │  应用服务器 N │
    │  (Node.js)   │      │  (Node.js)   │      │  (Node.js)   │
    └──────┬──────┘      └──────┬──────┘      └──────┬──────┘
           │                     │                     │
           └─────────────────────┼─────────────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │   PostgreSQL 主从      │
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │       Redis 集群       │
                    └─────────────────────────┘
```

### 1.2 环境配置

| 环境 | 用途 | 域名 |
|------|------|------|
| development | 本地开发 | localhost |
| staging | 预发布测试 | staging.novel-studio-web.com |
| production | 生产环境 | novel-studio-web.com |

---

## 2. 环境要求

### 2.1 服务器要求

#### 应用服务器
- **操作系统**: Ubuntu 22.04 LTS / Debian 11
- **CPU**: 2 核心以上
- **内存**: 4GB 以上
- **存储**: 20GB SSD
- **Node.js**: 18.x / 20.x
- **pnpm**: 8.x

#### 数据库服务器
- **PostgreSQL**: 15+
- **内存**: 4GB 以上
- **存储**: 50GB SSD（可扩展）

#### Redis 服务器
- **Redis**: 7+
- **内存**: 2GB 以上

### 2.2 依赖软件

```bash
# Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# pnpm
npm install -g pnpm

# PostgreSQL
sudo apt-get install -y postgresql postgresql-contrib

# Redis
sudo apt-get install -y redis-server

# Nginx
sudo apt-get install -y nginx

# PM2 (进程管理)
npm install -g pm2
```

---

## 3. 本地开发部署

### 3.1 项目克隆

```bash
git clone https://github.com/your-username/Novel-Studio-Web.git
cd Novel-Studio-Web
```

### 3.2 环境变量配置

#### 前端配置 (`client/.env`)
```bash
# API 地址
VITE_API_BASE_URL=http://localhost:3001
VITE_WS_BASE_URL=ws://localhost:3001

# 应用配置
VITE_APP_NAME=Novel Studio Web
VITE_APP_VERSION=1.0.0

# 第三方服务
VITE_GOOGLE_CLIENT_ID=your-google-client-id
VITE_GITHUB_CLIENT_ID=your-github-client-id
```

#### 后端配置 (`server/.env`)
```bash
# 应用配置
NODE_ENV=development
PORT=3001
HOST=0.0.0.0

# 数据库
DATABASE_URL=postgresql://user:password@localhost:5432/novel_ide_web

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_ACCESS_SECRET=your-access-secret-key
JWT_REFRESH_SECRET=your-refresh-secret-key
JWT_ACCESS_EXPIRES_IN=900
JWT_REFRESH_EXPIRES_IN=604800

# AI 配置
OPENAI_API_KEY=your-openai-api-key
ANTHROPIC_API_KEY=your-anthropic-api-key
DEEPSEEK_API_KEY=your-deepseek-api-key

# OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# 文件存储
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# 邮件（可选）
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=password
SMTP_FROM=noreply@novel-studio-web.com
```

### 3.3 数据库初始化

```bash
# 进入后端目录
cd server

# 安装依赖
pnpm install

# 运行数据库迁移
pnpm prisma migrate dev

# 填充种子数据（可选）
pnpm prisma db seed
```

### 3.4 启动开发服务器

#### 启动后端
```bash
cd server
pnpm dev
```

#### 启动前端（新终端）
```bash
cd client
pnpm dev
```

访问:
- 前端: http://localhost:3000
- 后端 API: http://localhost:3001
- API 文档: http://localhost:3001/docs (Swagger)

---

## 4. Docker 部署

### 4.1 Docker Compose 配置

#### `docker-compose.yml`
```yaml
version: '3.8'

services:
  # PostgreSQL 数据库
  postgres:
    image: postgres:15-alpine
    container_name: novel-ide-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis 缓存
  redis:
    image: redis:7-alpine
    container_name: novel-ide-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # 后端服务
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: novel-ide-server
    restart: unless-stopped
    environment:
      NODE_ENV: ${NODE_ENV}
      PORT: 3001
      DATABASE_URL: postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}
      REDIS_URL: redis://redis:6379
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      # ... 其他环境变量
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "3001:3001"
    volumes:
      - ./uploads:/app/uploads

  # 前端服务
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: novel-ide-client
    restart: unless-stopped
    environment:
      VITE_API_BASE_URL: ${API_BASE_URL}
      VITE_WS_BASE_URL: ${WS_BASE_URL}
    depends_on:
      - server
    ports:
      - "3000:80"

volumes:
  postgres_data:
  redis_data:
```

#### `server/Dockerfile`
```dockerfile
FROM node:20-alpine AS builder

WORKDIR /app

# 安装依赖
COPY package.json pnpm-lock.yaml ./
RUN npm install -g pnpm && pnpm install

# 复制源代码
COPY . .

# 构建 TypeScript
RUN pnpm build

# 生产镜像
FROM node:20-alpine

WORKDIR /app

# 安装生产依赖
COPY package.json pnpm-lock.yaml ./
RUN npm install -g pnpm && pnpm install --prod

# 复制构建产物
COPY --from=builder /app/dist ./dist

# 创建上传目录
RUN mkdir -p uploads

EXPOSE 3001

CMD ["node", "dist/main.js"]
```

#### `client/Dockerfile`
```dockerfile
FROM node:20-alpine AS builder

WORKDIR /app

# 安装依赖
COPY package.json pnpm-lock.yaml ./
RUN npm install -g pnpm && pnpm install

# 复制源代码
COPY . .

# 构建
RUN pnpm build

# 生产镜像
FROM nginx:alpine

# 复制构建产物
COPY --from=builder /app/dist /usr/share/nginx/html

# 复制 nginx 配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

#### `client/nginx.conf`
```nginx
server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    # SPA 路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 代理（如果需要）
    location /api {
        proxy_pass http://server:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # WebSocket 代理
    location /ws {
        proxy_pass http://server:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
    }
}
```

### 4.2 启动服务

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑环境变量
vim .env

# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down

# 停止并删除数据
docker-compose down -v
```

---

## 5. 生产环境部署

### 5.1 服务器准备

#### 初始化脚本 (`scripts/server-setup.sh`)
```bash
#!/bin/bash

set -e

echo "开始服务器初始化..."

# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装基础软件
sudo apt install -y curl git vim ufw fail2ban

# 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# 安装 pnpm
npm install -g pnpm

# 安装 PM2
npm install -g pm2

# 配置防火墙
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable

echo "服务器初始化完成！"
```

### 5.2 数据库部署

#### PostgreSQL 主从配置

```bash
# 主库配置
sudo vim /etc/postgresql/15/main/postgresql.conf

# 添加以下配置
listen_addresses = '*'
wal_level = replica
max_wal_senders = 3
wal_keep_size = 1GB

# 创建复制用户
sudo -u postgres psql
CREATE USER replica_user WITH REPLICATION ENCRYPTED PASSWORD 'password';
\q

# 配置 pg_hba.conf
sudo vim /etc/postgresql/15/main/pg_hba.conf

# 添加
host    replication     replica_user        0.0.0.0/0               md5
```

### 5.3 应用部署

#### 使用 PM2 部署

#### `ecosystem.config.js`
```javascript
module.exports = {
  apps: [
    {
      name: 'novel-ide-server',
      script: 'dist/main.js',
      cwd: '/opt/novel-studio-web/server',
      instances: 'max',
      exec_mode: 'cluster',
      env: {
        NODE_ENV: 'production',
        PORT: 3001,
      },
      env_production: {
        NODE_ENV: 'production',
        PORT: 3001,
      },
      error_file: './logs/err.log',
      out_file: './logs/out.log',
      log_date_format: 'YYYY-MM-DD HH:mm:ss',
      merge_logs: true,
      autorestart: true,
      max_memory_restart: '1G',
    },
  ],
};
```

#### 部署脚本 (`scripts/deploy.sh`)
```bash
#!/bin/bash

set -e

APP_DIR="/opt/novel-studio-web"
GIT_REPO="https://github.com/your-username/Novel-Studio-Web.git"

echo "开始部署..."

# 拉取代码
if [ ! -d "$APP_DIR" ]; then
  git clone $GIT_REPO $APP_DIR
else
  cd $APP_DIR
  git pull origin main
fi

# 安装依赖
cd $APP_DIR/server
pnpm install --prod

# 运行迁移
pnpm prisma migrate deploy

# 构建
pnpm build

# 重启 PM2
pm2 reload ecosystem.config.js

echo "部署完成！"
```

### 5.4 Nginx 配置

#### `/etc/nginx/sites-available/novel-studio-web`
```nginx
# HTTP 重定向到 HTTPS
server {
    listen 80;
    server_name novel-studio-web.com www.novel-studio-web.com;

    return 301 https://$server_name$request_uri;
}

# HTTPS 配置
server {
    listen 443 ssl http2;
    server_name novel-studio-web.com www.novel-studio-web.com;

    # SSL 证书
    ssl_certificate /etc/letsencrypt/live/novel-studio-web.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/novel-studio-web.com/privkey.pem;

    # SSL 配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # 日志
    access_log /var/log/nginx/novel-studio-web-access.log;
    error_log /var/log/nginx/novel-studio-web-error.log;

    # 前端静态文件
    location / {
        root /opt/novel-studio-web/client/dist;
        try_files $uri $uri/ /index.html;

        # 缓存配置
        expires 1h;
        add_header Cache-Control "public, immutable";
    }

    # API 代理
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $host;
    }

    # WebSocket 代理
    location /ws {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 文件上传大小限制
    client_max_body_size 10M;
}
```

#### 启用配置
```bash
sudo ln -s /etc/nginx/sites-available/novel-studio-web /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### 5.5 SSL 证书配置

```bash
# 安装 Certbot
sudo apt install -y certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d novel-studio-web.com -d www.novel-studio-web.com

# 自动续期
sudo certbot renew --dry-run
```

---

## 6. 监控与日志

### 6.1 应用监控

#### PM2 Plus（可选）
```bash
pm2 link <secret_key> <public_key>
```

#### 自定义监控
```typescript
// 监控健康状态
app.get('/health', (req, res) => {
  res.json({
    status: 'ok',
    timestamp: new Date().toISOString(),
    uptime: process.uptime(),
    memory: process.memoryUsage(),
  });
});
```

### 6.2 日志管理

#### 日志轮转 (`/etc/logrotate.d/novel-studio-web`)
```
/opt/novel-studio-web/logs/*.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    create 0644 node node
}
```

#### 集中式日志（可选 - ELK Stack）

---

## 7. 备份策略

### 7.1 数据库备份

#### 备份脚本 (`scripts/backup-db.sh`)
```bash
#!/bin/bash

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/backup/postgres"
BACKUP_FILE="$BACKUP_DIR/novel_ide_web_$DATE.sql"

mkdir -p $BACKUP_DIR

# 备份数据库
pg_dump -U postgres -d novel_ide_web > $BACKUP_FILE

# 压缩
gzip $BACKUP_FILE

# 删除 30 天前的备份
find $BACKUP_DIR -name "*.sql.gz" -mtime +30 -delete

echo "备份完成: $BACKUP_FILE.gz"
```

#### 定时任务
```bash
# 编辑 crontab
crontab -e

# 每天凌晨 2 点备份
0 2 * * * /opt/novel-studio-web/scripts/backup-db.sh
```

### 7.2 文件备份

```bash
# 备份上传文件
rsync -avz /opt/novel-studio-web/uploads /backup/uploads/
```

---

## 8. 性能优化

### 8.1 Nginx 优化

```nginx
# worker 进程
worker_processes auto;

# 连接数
events {
    worker_connections 4096;
}

# 缓存
proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=api_cache:10m max_size=1g inactive=60m;

server {
    # API 缓存
    location /api/ai/models {
        proxy_cache api_cache;
        proxy_cache_valid 200 5m;
        proxy_pass http://localhost:3001;
    }
}
```

### 8.2 数据库优化

```sql
-- 调整 PostgreSQL 配置
ALTER SYSTEM SET shared_buffers = '2GB';
ALTER SYSTEM SET effective_cache_size = '6GB';
ALTER SYSTEM SET maintenance_work_mem = '512MB';
ALTER SYSTEM SET checkpoint_completion_target = 0.9;
ALTER SYSTEM SET wal_buffers = '16MB';
ALTER SYSTEM SET default_statistics_target = 100;
```

---

## 9. 故障排查

### 9.1 常见问题

#### 应用无法启动
```bash
# 检查端口占用
sudo netstat -tlnp | grep :3001

# 查看日志
pm2 logs novel-ide-server

# 检查环境变量
pm2 env 0
```

#### 数据库连接失败
```bash
# 检查 PostgreSQL 状态
sudo systemctl status postgresql

# 检查连接
sudo -u postgres psql -l

# 测试连接
psql -U postgres -d novel_ide_web -h localhost
```

#### SSL 证书过期
```bash
# 手动续期
sudo certbot renew

# 测试配置
sudo nginx -t
sudo systemctl reload nginx
```

---

## 10. CI/CD 配置

### 10.1 GitHub Actions

#### `.github/workflows/deploy.yml`
```yaml
name: Deploy to Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Run tests
        run: pnpm test

      - name: Deploy to server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /opt/novel-studio-web
            git pull origin main
            cd server
            pnpm install --prod
            pnpm prisma migrate deploy
            pnpm build
            pm2 reload ecosystem.config.js
```

---

**文档版本**: v1.0
**最后更新**: 2026-02-11
