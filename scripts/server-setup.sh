#!/usr/bin/env bash
set -euo pipefail

echo "Install Docker, Docker Compose plugin, Node.js 20 and pnpm on the target server before deployment."
echo "Then run scripts/deploy.sh from the project root."